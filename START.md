# 返利导购平台 · 本地启动

三个终端窗口，分别跑：

```bash
# 1. 服务端  → http://localhost:3000/docs
cd server && npm install && npm run seed && npm run dev

# 2. H5 用户端 → http://localhost:5173
cd h5 && npm install && npm run dev

# 3. 管理后台 → http://localhost:5174
cd admin && npm install && npm run dev
```

服务端必须先起，H5 和后台的 `/api` 都代理到 3000。

## 演示账号

| 端 | 账号 | 说明 |
| --- | --- | --- |
| 管理后台 | `admin` / `admin123` | |
| H5 普通用户 | `13900000001` | 挂在代理1名下 |
| H5 代理 | `13800000001` | 有推广码和二维码 |

验证码：点「获取验证码」会自动填入（开发模式服务端直接返回，接短信服务商后删掉 `devCode` 即可）。

## 想看数据效果

跑一遍 `cd server && npm run simulate`，会造一笔完整订单走完
下单 → 结算 → 分佣 → 提现 → 退款冲销，然后 H5 的「我的 / 订单 / 资金明细」
和后台的「订单管理 / 提现审核」就都有东西了。

## 走一遍完整体验

1. 后台 → 代理管理，看两个代理的业绩和分成
2. H5 用 `13800000001` 登录 → 我的 → 推广中心，拿到专属二维码
3. 手机扫码（或直接开 `http://localhost:5173/#/i/推广码`）→ 落地页 → 注册
4. 新用户下单 → 后台「订单管理」手动拉单 → 分佣自动入账
5. H5 提现 → 后台「提现审核」通过 → 登记打款
6. 后台「仪表盘」→ 立即对账，验证流水与余额一致

## 目录

```
fanly/
├── server/   NestJS 服务端（已验收全链路）
├── h5/       Vue3 + Vant 用户端
└── admin/    Vue3 + Element Plus 管理后台
```

## 局域网 / 手机真机访问

两个前端的 `vite.config.js` 已经设了 `host: true`，重启 dev server 后会同时监听局域网地址，
启动日志里的 `Network:` 那一行就是手机能用的地址。

先查本机 IP：

```bash
ipconfig getifaddr en0     # Wi-Fi；有线网用 en1
```

本机当前是 `192.168.68.163`（换网络后要重查）：

| 端 | 手机/其他电脑访问 |
| --- | --- |
| H5 | http://192.168.68.163:5173 |
| 管理后台 | http://192.168.68.163:5174 |

**要改一处配置**：`server/.env` 里的 `H5_BASE_URL` 决定了推广二维码里写的地址，
还是 localhost 的话手机扫出来打不开。改成：

```
H5_BASE_URL=http://192.168.68.163:5173
```

改完重启服务端。

### 注意

- 手机和电脑必须在同一个 Wi-Fi，且路由器没开 AP 隔离
- 首次启动 macOS 可能弹防火墙询问，选「允许」
- 服务端 3000 端口不用对外暴露 —— 前端的 `/api` 请求是 Vite dev server 在你电脑上转发的，
  手机只跟 5173 / 5174 打交道
- IP 会随路由器 DHCP 变，换了网络要重新查一次并改 `H5_BASE_URL`
