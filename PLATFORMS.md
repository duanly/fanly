# 接入各电商平台 · 申请指南

四家的门槛差别很大。按「个人/小团队能不能独立拿下」排序：

| 平台 | 直连难度 | 关键卡点 | 建议路径 |
| --- | --- | --- | --- |
| **拼多多** | ★☆☆ | 推广位备案，每个用户一次授权 | **已接通**，直连 |
| **京东** | ★★☆ | API 权限要申请，`subUnionId` 有流量门槛 | **直连**，用 positionId 兜底 |
| **抖音** | ★★★ | 精选联盟开放能力对个人基本不开 | 走聚合商 |
| **淘宝** | ★★★★ | 媒体资质 + 渠道备案，个人几乎拿不到 | **走聚合商**（订单侠 key 已有） |

> 下面的后台菜单名可能和你看到的对不上——各家改版很勤。
> 找不到就在后台搜「API」「推广位」「开放平台」这几个词。

---

## 京东联盟

### 一、注册联盟账号

1. [union.jd.com](https://union.jd.com) 注册，选**个人**或**企业**
2. 实名认证（个人身份证即可）
3. 填结算信息（银行卡），不填拿不到佣金

个人账号就能推广，这一步没有门槛。

### 二、建推广位，拿 positionId

后台「推广管理 → 推广位管理」新建推广位，类型选**网站/APP**。
建完会给一个数字 ID，就是 `positionId`。

**先建一个自用的**，用来跑通接口；正式运营时每个用户单独建（见第四节）。

### 三、申请 API 权限，拿 appKey / appSecret

这一步是关键。两条路：

**路径 A：联盟后台直接申请**
后台找「API 权限」或「开发者」入口，提交应用信息。审核通过后给 `appKey` / `appSecret`。

**路径 B：京东开放平台**
[open.jd.com](https://open.jd.com) 创建应用，类目选「联盟开放平台」，
再回联盟后台把应用和联盟账号绑定。

提交时会问「推广场景」和「日均流量」。**如实填**，
夸大了后面对不上反而麻烦；量小也能过，只是高级接口不给。

需要的接口权限（申请时勾选）：

| 接口 | 用途 |
| --- | --- |
| `jd.union.open.goods.query` | 商品查询（关键词搜索） |
| `jd.union.open.goods.jingfen.query` | 京粉精选（榜单/选品池） |
| `jd.union.open.promotion.bysubunionid.get` | 转链（带归属参数） |
| `jd.union.open.order.row.query` | 订单行查询（拉单对账） |

### 四、订单归属：subUnionId 还是 positionId

这是京东最需要提前想清楚的事。

**`subUnionId`** 是给每个用户带一个自定义标识，一个推广位服务所有用户——
干净、无上限，但**要单独申请权限，有流量门槛**（日 UV、月订单、月 GMV 都要够）。
新站基本申请不下来。

**`positionId`** 是给每个用户建一个推广位，靠推广位反查用户。
不用申请，立刻能用，但**推广位数量有上限**（约 5000 个）。

**结论**：先用 positionId，量起来了再申请 subUnionId 换过去。
我们的 `promotion_position` 表本来就是每用户每平台一条，换的时候业务代码不用动。

### 五、技术上的坑（写代码时会撞到）

- **网关**：`https://api.jd.com/routerjson`，POST 表单
- **时间戳**格式是 `yyyy-MM-dd HH:mm:ss` 的**北京时间字符串**，不是 Unix 秒
- **签名** = `MD5(appSecret + 按 key 升序拼接的 key+value + appSecret)` 转大写，跟拼多多同构
- **响应里套了一层 JSON 字符串**：外层拿到 `xxx_responce.result` 之后
  还要再 `JSON.parse` 一次才是数据。这个最容易漏
- **响应 key 拼写是 `responce`**（京东自己拼错的，沿用至今），别按 `response` 找
- **金额单位是元**，不是分（跟拼多多相反）；佣金比例是百分数（`5.0` = 5%）

---

## 淘宝联盟（阿里妈妈）

### 现实情况

个人想直连淘宝客 API，2020 年之后基本走不通了：

1. **要媒体资质** —— 网站要 ICP 备案且能打开，APP 要上架，公众号要认证
2. **物料/选品接口要服务商权限** —— 普通淘客只能拿到很有限的几个接口
3. **渠道备案（`relation_id` / `special_id`）要单独开通** —— 而订单归属正是靠它

第 3 条是死结：没有渠道备案，订单认不到具体用户，返利就发不出去。

### 建议路径

**走聚合服务商**。你手上的订单侠 key 就是干这个的——
聚合商已经拿到了渠道备案资质，我们通过它转链，订单归属靠它透传我们的参数。

代价是佣金被抽一道（通常 5%~10%），换来的是不用自己啃资质。

要做的事：
1. 登录订单侠后台，完成**淘宝授权**（绑定阿里妈妈账号、填 PID/adzoneId）
2. 跟客服确认两件事：
   - 转链是否支持 `relation_id` / `special_id` 透传
   - 怎么开通渠道备案（有些聚合商代办）
3. 回来跑 `deploy/probe-agg.sh` 验证

### 如果坚持直连

先去 [pub.alimama.com](https://pub.alimama.com) 注册并实名，
在「媒体管理」提交网站资质（用 `fanly.yytbank.cn`，已备案），
通过后再去 [open.taobao.com](https://open.taobao.com) 创建应用、应用类型选「淘宝客」。

**先把媒体资质过了再谈 API**，顺序反了会白折腾。

---

## 抖音（精选联盟）

开放能力目前主要面向机构和有量的达人，个人申请通过率低。
建议**先放着**，等前三家跑顺了再说，或者同样走聚合商。

---

## 接入新平台时，我们这边要动什么

好消息是业务层不用动。`CpsProvider` 接口只有五个方法：

```
searchGoods / getGoodsDetail / convertLink / parseContent / fetchOrders
```

加一家平台 = 写一个 `providers/xx.provider.ts` 实现这五个方法，
在 `cps.service.ts` 注册，`.env` 填 key。

分佣引擎、订单表、选品池、比价组全都不认平台，一行不改。

**已经预留好的位置**：

```
.env
  JD_APP_KEY / JD_APP_SECRET / JD_SITE_ID
  TB_APP_KEY / TB_APP_SECRET / TB_ADZONE_ID
  DY_APP_KEY / DY_APP_SECRET
```

填了就切真实渠道，不填就是 mock。

---

## 拿到 key 之后的验证顺序

跟拼多多那次一样，照这个顺序走，每步不通就别往下：

1. **搜索** —— 签名对不对、权限有没有
2. **详情** —— 字段映射对不对
3. **转链** —— ★ 订单归属的命门，出短链才算过
4. **下单** —— 用转出来的链真买一单
5. **拉单** —— 几分钟后看自定义参数有没有原样回来

第 5 步回得来，这个平台就算接通了。
